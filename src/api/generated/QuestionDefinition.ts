// Generated by crates/xtask from the Rust question model.
// Do not edit by hand; run ./build.sh (or cargo run -p xtask -- tsgen).

import type { AttemptPolicy } from "./AttemptPolicy";
import type { ContentBlock } from "./ContentBlock";
import type { GradingDefinition } from "./GradingDefinition";
import type { ProblemId } from "./ProblemId";
import type { QuestionMetadata } from "./QuestionMetadata";
import type { QuestionSource } from "./QuestionSource";
import type { RandomizationDefinition } from "./RandomizationDefinition";
import type { ResponseDefinition } from "./ResponseDefinition";
import type { TimingPolicy } from "./TimingPolicy";
import type { VersionId } from "./VersionId";
import type { WorkspaceId } from "./WorkspaceId";

/**
 * A question, in the platform's own representation.
 *
 * The `problem` field is what separates a draft from published content: a
 * draft lives in a workspace and has no [`ProblemId`], and publishing is the
 * transition that assigns one. Reading `problem.is_none()` is therefore the
 * same question as "is this a draft", with no separate flag to fall out of
 * sync.
 */
export type QuestionDefinition = {
  version: VersionId;
  problem: ProblemId | null;
  workspace: WorkspaceId;
  source: QuestionSource;
  prompt: Array<ContentBlock>;
  response: ResponseDefinition;
  attemptPolicy: AttemptPolicy;
  timingPolicy: TimingPolicy;
  randomization: RandomizationDefinition;
  grading: GradingDefinition;
  metadata: QuestionMetadata;
};
