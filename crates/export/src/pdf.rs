//! PDF writer (MOD-EXPORT).
//!
//! Implemented in M3. A question whose capabilities mark it unexportable --
//! anything interactive that cannot be represented on paper -- is flagged
//! before the build starts, so the artifact never contains a silent
//! placeholder where a question should be.
