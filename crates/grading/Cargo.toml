# Server-only Answer Keys, checkers, and correctness decisions.
# Security boundary: this crate is server-only. It is absent from the
# `crates/wasm` dependency closure, which makes shipping an answer to the
# browser a compile-time impossibility rather than a review question.
[package]
name = "grading"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
question_model.workspace = true
domain.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true

[dev-dependencies]
serde_json.workspace = true
uuid.workspace = true
