# H5P package import for ungraded practice. Declares serverGrading: false,
# so it never reaches `grading`.
[package]
name = "adapter_h5p"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
async-trait.workspace = true
question_model.workspace = true
serde.workspace = true
sha2.workspace = true
uuid.workspace = true

[dev-dependencies]
tokio.workspace = true
