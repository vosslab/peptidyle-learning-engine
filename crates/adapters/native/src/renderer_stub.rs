//! Response-widget rendering hooks for the native adapter.
//!
//! Implemented in M3 alongside MOD-UI-WIDGETS. Answer collection happens here;
//! answer checking never does, because that belongs to `grading`.
