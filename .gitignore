# === UNIVERSAL ===
.DS_Store
report_*.txt
output*/
_temp*.?*
/OTHER_REPOS/
/LOCAL_ONLY/

# === LOCAL ===
# Local container credentials. containers/env.example is the committed
# template; the .local file holds real values and must never be committed.
/_temp/
/generated/
containers/env.local
containers/local-identities.json
containers/local-login.txt
containers/local-demo.json
containers/local-webwork-demo.json
containers/local-chapter-one-pilot.json
containers/.runtime/
containers/.secrets/
.env
# === PYTHON ===
build/
dist/
sdist/
*.egg
*.egg-info/
site/

# === TYPESCRIPT ===
node_modules/
dist_*/
_site/
*.tsbuildinfo
.eslintcache
.prettiercache
test-results/
playwright-report/
blob-report/
coverage/
meta.json
stats.html

# === RUST ===
/target/
**/target/
**/*.rs.bk
.rust_analyzer/

# === OPENTOFU ===
# Provider binaries and module caches are platform-local generated state.
# Keep the reviewed provider checksum lockfile under deploy/opentofu tracked.
**/.terraform/




