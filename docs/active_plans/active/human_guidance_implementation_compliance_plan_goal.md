/goal Implement docs/active_plans/active/human_guidance_implementation_compliance_plan.md so the running PLE system matches every product-behavior bullet of docs/HUMAN_GUIDANCE.md, a verbatim generated checklist records verified evidence for each bullet, a fresh set of implementation compliance reports summarizes what the code audit found, and the only open items are HG-unlocked designs or genuinely ambiguous product readings recorded for Neil. The finished state is reached by the manager and subagents alone: observation first, then small evidence-backed correction milestones, then re-audit of the same checklist. Human Guidance is the authority and the docs-pass compliance reports are context. The plan is the primary source for scope and implementation detail.
