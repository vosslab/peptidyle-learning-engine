/goal Implement docs/active_plans/active/record_list_page_frame_standardization_plan.md so pages describe record content and behavior through an expressive shared family that owns consistent presentation and reflows the same content at every width. Complete every caller migration, standardize PageFrame layout, and provide logical ancestry and loaded navigation names. Keep presentation in Solid/TypeScript and complete the bounded data workstream: PostgreSQL/API own broad-query filtering, sorting and paging; shared discovery controls offer 50/100/250 records with default 50. Reuse bounded Assessment pickers and retain only demonstrated rendering machinery. Preserve real task semantics, useful media and descriptions, selection, actions, sorting, ordering and editing. Remove superseded APIs, caller styling, duplicate presentation machinery, staging code and temporary evidence. The manager and subagents complete the plan autonomously. The plan is the primary source for scope and implementation detail, under docs/HUMAN_GUIDANCE.md and docs/REPO_STYLE.md.
