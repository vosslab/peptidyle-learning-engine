/goal Implement docs/active_plans/semantic_naming_alignment.md so PLE has one coherent, Human-Guidance-aligned Id/Tuple/EditNumber/RevisionNumber model across PostgreSQL, Rust, generated contracts, JSON/API, TypeScript, fixtures, and living documentation; exact revisions and scoped identities are typed canonical contracts, HTTP ETag is only header encoding, and the durable contract registry protects only stable cross-boundary behavior. Favor the smallest robust design consistent with `docs/REPO_STYLE.md`. The plan is the primary source for scope and implementation detail, and `docs/HUMAN_GUIDANCE.md` is the product authority.
